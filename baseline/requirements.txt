# baseline/requirements.txt
# Dependencies for running the baseline inference script.
# The server must be running separately (see server/requirements.txt).

openai==1.30.1
websockets==12.0
pydantic==2.7.1
openenv-core>=0.1.0
pytest>=8.0
